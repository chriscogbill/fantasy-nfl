# Server
PORT=3000
SESSION_SECRET=change-me-to-a-random-secret

# Database
DB_USER=chriscogbill
DB_HOST=localhost
DB_NAME=fantasyNFL
DB_PASSWORD=
DB_PORT=5432

# Shared auth database (must match cogs-auth service)
AUTH_DB_NAME=cogsAuth

# CORS origins (comma-separated)
ALLOWED_ORIGINS=http://localhost:3001,http://localhost:3002

# Set in production for cross-subdomain cookies
# COOKIE_DOMAIN=.cogs.tech
