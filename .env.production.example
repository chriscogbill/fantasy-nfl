# ============================================
# Production Environment Variables
# ============================================
# Copy this file and set values for your production environment.
# For Railway: set these in the Railway dashboard per service.

# ---- Express API Service ----
NODE_ENV=production
PORT=3000

# Main database (fantasyNFL)
DATABASE_URL=postgresql://user:password@host:5432/fantasyNFL

# Auth/session database (cogsAuth)
AUTH_DATABASE_URL=postgresql://user:password@host:5432/cogsAuth

# Session secret (must match across API and cogs-auth services)
SESSION_SECRET=generate-a-strong-random-secret-here

# CORS - frontend origin(s), comma-separated
ALLOWED_ORIGINS=https://fantasynfl.cogs.tech

# Cookie domain for cross-subdomain session sharing
COOKIE_DOMAIN=.cogs.tech

# ---- Next.js Frontend Service ----
# These are build-time variables (NEXT_PUBLIC_ prefix)
NEXT_PUBLIC_API_URL=https://api-fantasynfl.cogs.tech
NEXT_PUBLIC_AUTH_URL=https://auth.cogs.tech
